FROM apache/superset:latest

USER root
# Install psycopg2 into the specific pip environment that Superset's virtualenv uses
RUN pip install --target=/app/.venv/lib/python3.10/site-packages psycopg2-binary
USER superset
