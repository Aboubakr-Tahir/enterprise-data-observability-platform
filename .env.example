# --- Airflow Configuration ---
# On Linux/macOS, set this to $(id -u)
# On Windows, 50000 is usually fine
AIRFLOW_UID=50000

# --- Superset Configuration ---
# Generate a long random string for production
SUPERSET_SECRET_KEY=y0ur_s3cr3t_k3y_h3r3

# --- Optional: Custom Database URLs (if not using defaults) ---
# AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
# SUPERSET_SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://airflow:airflow@postgres/airflow
